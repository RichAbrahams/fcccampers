import React from 'react';
import ErrorWrapper from '../styledComponents/ErrorWrapper';
import ErrorText from '../styledComponents/ErrorText';

export default () => (
  <ErrorWrapper>
    <ErrorText>No channels match your filter</ErrorText>
  </ErrorWrapper>
);
