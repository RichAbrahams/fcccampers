import styled from 'styled-components';

const stc = styled.span`
  position: relative;
  bottom: 2px;
  font-size: 12px;
  line-height: 12px;
  color: rgb(244, 67, 54);
  transition: all 450ms cubic-bezier(0.23, 1, 0.32, 1) 0ms;
`;

export default stc;
